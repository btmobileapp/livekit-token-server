{
  "name": "livekit-token-server",
  "version": "1.0.0",
  "description": "LiveKit token server for video calling app",
  "main": "token_server.js",
  "scripts": {
    "start": "node token_server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "livekit-server-sdk": "^2.0.0"
  }
}
